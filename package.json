{
  "name": "flowpilot",
  "version": "0.1.0",
  "private": true,
  "description": "FlowPilot - Build browser automations visually.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/server & npm run dev --workspace=apps/web & wait",
    "dev:server": "npm run dev --workspace=apps/server",
    "dev:web": "npm run dev --workspace=apps/web",
    "build": "npm run build --workspace=packages/shared && npm run build --workspace=packages/workflow-schema && npm run build --workspace=packages/browser-engine && npm run build --workspace=packages/workflow-engine && npm run build --workspace=apps/server && npm run build --workspace=apps/web",
    "start": "npm run start --workspace=apps/server",
    "test": "npm run test --workspaces --if-present",
    "db:seed": "tsx prisma/seed.ts"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "tsx": "^4.16.2",
    "@types/node": "^22.10.5",
    "vitest": "^2.0.5"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
